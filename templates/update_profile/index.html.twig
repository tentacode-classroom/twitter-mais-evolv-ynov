{% extends 'base.html.twig' %}

{% block title %}Modifier le profil{% endblock %}

{% block body %}
<div class="container page_container_user_view">
    <div class="row justify-content-center">
        <div class="col-5">
            <ul>
                <li><a href="{{ path('update_password') }}">Changer le mot de passe</a></li>
            </ul>
            <h2>Modifier le profil - {{ app.user.firstname }} {{ app.user.lastname }}</h2>
            <br>
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
        </div>
    </div>

</div>
{% endblock %}
